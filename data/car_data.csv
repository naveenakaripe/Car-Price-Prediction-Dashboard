import pandas as pd
import numpy as np

np.random.seed(42)

n = 100

df = pd.DataFrame({
    'Year': np.random.randint(2010, 2024, n),
    'Fuel_Type': np.random.choice(['Petrol', 'Diesel'], n),
    'Mileage': np.random.randint(10000, 100000, n),
})

df['Price'] = (
    1000000
    - (2024 - df['Year']) * 40000
    - df['Mileage'] * 2
    + np.random.randint(-50000, 50000, n)
)

df.to_csv('car_data.csv', index=False)

print(df.head())
